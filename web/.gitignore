# build output
openbw_play
*.wasm
*.dSYM/
build.err
build.log
__pycache__/

# downloaded game assets (copyrighted — fetched locally for dev only; the
# browser build fetches these client-side)
data/

# downloaded test maps — keep only the committed CC BY 4.0 map + its attribution
maps/*
!maps/Melancholy_v1.scx
!maps/ATTRIBUTION.md

# render dumps
*.ppm
*.png
